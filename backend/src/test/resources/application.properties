# Background schedulers must not compete with deterministic integration tests
# that share the test database. Tests that exercise the lease protocol enable
# the worker flag explicitly and drive claim/process themselves.
rag.chat.context-compression.worker-enabled=false
